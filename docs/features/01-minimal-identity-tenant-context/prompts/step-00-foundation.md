# Step 0 Prompt — Foundation and Contracts

## Tests first

Create only tests/checks for reproducible scaffold commands, import boundaries,
Django-only migrations, non-owner API role, OpenAPI generation/client compile, and
manifest drift. Do not add F-001 behavior.

## Implementation

After independent test review, implement only Step 0 from `implementation-plan.md`.
Own shared manifests/locks/composition/CI/contracts, select current supported versions
with primary-source evidence, and make the planned Make targets executable. Do not add
membership, invitation, PDF, worker, AI, provider credentials, or production data.
