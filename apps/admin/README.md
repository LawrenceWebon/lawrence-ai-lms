# Admin process

Run the Django Admin foundation entrypoint with:

```text
cd backend
PYTHONPATH=src uv run python manage.py runserver
```

Step 0 contains no tenant administration action or alternate authorization path.
