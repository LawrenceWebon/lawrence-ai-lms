{
  "schema_version": 1,
  "authority": "Django",
  "schema_fingerprint": "sha256:2390502c971d303b736a28acc734e3a86afdd0f1116e89804f7570879df5102e",
  "database_fingerprint": "sha256:ba236c2c97c71402ec745ebf503683ceb8c8158af975ba918e3c65e6d08b8d02",
  "objects": [
    {
      "schema": "app",
      "table": "source_documents",
      "migration": "documents.0001_initial",
      "security_migration": "documents.0002_document_security",
      "columns": ["id", "tenant_id", "display_name", "owner_actor_id", "current_version_id", "row_version", "created_at", "updated_at"],
      "keys": ["PRIMARY KEY (id)", "UNIQUE (tenant_id,id)", "FOREIGN KEY (tenant_id,current_version_id) -> source_document_versions(tenant_id,id)"],
      "checks": ["row_version >= 1", "current version is tenant-scoped"],
      "indexes": ["ix_source_docs_owner (tenant_id,owner_actor_id)"],
      "lifecycle": "stable source identity; current version pointer is service-controlled",
      "rls": "ENABLE + FORCE"
    },
    {
      "schema": "app",
      "table": "source_document_versions",
      "migration": "documents.0001_initial",
      "security_migration": "documents.0002_document_security",
      "columns": ["id", "tenant_id", "source_document_id", "version_number", "admission_status", "declared_filename", "content_sha256", "derived_file_size_bytes", "derived_media_type", "derived_pdf_signature_valid", "derived_parser_accepted", "derived_page_count", "derived_max_rendered_pixels_per_page", "derived_rendered_pixels_total", "derived_decoded_parser_bytes", "derived_local_inspection_result", "rejection_code", "validation_attempt_count", "removal_status", "removal_reason_code", "row_version", "created_at", "updated_at"],
      "keys": ["PRIMARY KEY (id)", "UNIQUE (tenant_id,id)", "UNIQUE (tenant_id,source_document_id,id)", "UNIQUE (tenant_id,source_document_id,version_number)", "FOREIGN KEY (tenant_id,source_document_id) -> source_documents(tenant_id,id)"],
      "checks": ["fixed admission/removal states", "canonical checksum", "admitted evidence satisfies P-013", "rejection/removal shapes", "row_version >= 1"],
      "indexes": ["ix_source_versions_status (tenant_id,admission_status)"],
      "lifecycle": "identity and declared evidence immutable; terminal admission states cannot reopen",
      "rls": "ENABLE + FORCE"
    },
    {
      "schema": "app",
      "table": "source_rights_declarations",
      "migration": "documents.0001_initial",
      "security_migration": "documents.0002_document_security",
      "columns": ["id", "tenant_id", "source_document_id", "source_version_id", "declared_by_actor_id", "basis", "attestation_version", "attested_at", "rights_holder_name", "evidence_reference", "valid_until", "row_version"],
      "keys": ["PRIMARY KEY (id)", "UNIQUE (tenant_id,id)", "UNIQUE (tenant_id,source_version_id)", "composite tenant foreign keys to document and version"],
      "checks": ["one declaration per version"],
      "indexes": ["ix_source_rights_version (tenant_id,source_version_id)"],
      "lifecycle": "append-only immutable rights evidence",
      "rls": "ENABLE + FORCE"
    },
    {
      "schema": "app",
      "table": "source_use_authorizations",
      "migration": "documents.0001_initial",
      "security_migration": "documents.0005_ingestion_security",
      "columns": ["id", "tenant_id", "source_document_id", "source_version_id", "rights_declaration_id", "operation", "status", "requested_by_actor_id", "reviewed_by_actor_id", "decision_code", "valid_from", "valid_until", "row_version", "created_at", "updated_at"],
      "keys": ["PRIMARY KEY (id)", "UNIQUE (tenant_id,id)", "UNIQUE (tenant_id,source_version_id,operation)", "composite tenant foreign keys to document, version, and declaration"],
      "checks": ["operation in store/extract/ocr/generate", "fixed status", "reviewer separation", "decision/status shape"],
      "indexes": ["ix_source_auth_active (tenant_id,source_version_id,operation,status)"],
      "lifecycle": "separately reviewed operation-scoped authorization",
      "rls": "ENABLE + FORCE"
    },
    {
      "schema": "app",
      "table": "source_upload_intents",
      "migration": "documents.0001_initial",
      "security_migration": "documents.0002_document_security",
      "columns": ["id", "tenant_id", "source_document_id", "source_version_id", "authorization_id", "token_digest", "purpose", "status", "expires_at", "max_bytes", "accepted_media_type", "observed_content_sha256", "observed_byte_count", "upload_claim_digest", "upload_claim_expires_at", "row_version", "created_at", "updated_at"],
      "keys": ["PRIMARY KEY (id)", "UNIQUE (tenant_id,id)", "UNIQUE token digest", "UNIQUE active (tenant_id,source_version_id)", "composite tenant foreign keys to document, version, and authorization"],
      "checks": ["purpose = source_quarantine", "fixed status", "observation and upload-claim shapes", "canonical observed checksum"],
      "indexes": ["ix_source_intents_active (tenant_id,status,expires_at)", "uq_source_intents_active_version partial unique"],
      "lifecycle": "short-lived opaque single-version target; raw token is never persisted",
      "rls": "ENABLE + FORCE"
    },
    {
      "schema": "app",
      "table": "source_storage_objects",
      "migration": "documents.0001_initial",
      "security_migration": "documents.0002_document_security",
      "columns": ["id", "tenant_id", "source_document_id", "source_version_id", "upload_intent_id", "private_locator", "content_sha256", "byte_count", "media_type", "status", "observed_at", "removed_at", "row_version", "created_at", "updated_at"],
      "keys": ["PRIMARY KEY (id)", "UNIQUE (tenant_id,id)", "UNIQUE (tenant_id,source_version_id)", "UNIQUE upload intent", "UNIQUE private locator", "composite tenant foreign keys to document, version, and intent"],
      "checks": ["canonical checksum", "positive bytes", "PDF media", "fixed status", "removed-at shape"],
      "indexes": ["ix_source_objects_status (tenant_id,status)"],
      "lifecycle": "immutable observation; inventory is reconciled to missing/deleted, never row-deleted",
      "rls": "ENABLE + FORCE"
    },
    {
      "schema": "integration",
      "table": "document_jobs",
      "migration": "documents.0001_initial",
      "security_migration": "documents.0002_document_security",
      "columns": ["id", "tenant_id", "source_document_id", "source_version_id", "storage_object_id", "stage", "status", "reason_code", "attempt_count", "max_attempts", "lease_owner", "lease_expires_at", "checkpoint", "idempotency_key", "correlation_id", "input_manifest_sha256", "output_manifest_sha256", "row_version", "created_at", "updated_at"],
      "keys": ["PRIMARY KEY (id)", "UNIQUE (tenant_id,id)", "UNIQUE (tenant_id,source_version_id,stage)", "composite tenant foreign keys to document, version, and object"],
      "checks": ["fixed stage/status", "canonical manifest hashes", "lease shape", "1 <= max_attempts <= 10", "attempt_count <= max_attempts"],
      "indexes": ["ix_document_jobs_claim (tenant_id,stage,status)"],
      "lifecycle": "durable leased validation/removal checkpoint",
      "rls": "ENABLE + FORCE"
    },
    {
      "schema": "integration",
      "table": "document_job_attempts",
      "migration": "documents.0001_initial",
      "security_migration": "documents.0002_document_security",
      "columns": ["id", "tenant_id", "job_id", "attempt_number", "outcome", "retry_class", "reason_code", "inspector_version", "input_manifest_sha256", "output_manifest_sha256", "observation", "started_at", "completed_at"],
      "keys": ["PRIMARY KEY (id)", "UNIQUE (tenant_id,id)", "UNIQUE (tenant_id,job_id,attempt_number)", "FOREIGN KEY (tenant_id,job_id) -> document_jobs(tenant_id,id)"],
      "checks": ["fixed outcome", "observation is object", "canonical manifest hashes"],
      "indexes": ["ix_doc_attempts_job (tenant_id,job_id,attempt_number)"],
      "lifecycle": "immutable completed attempt evidence",
      "rls": "ENABLE + FORCE"
    },
    {
      "schema": "integration",
      "table": "document_ingestion_runs",
      "migration": "documents.0003_ingestion_schema",
      "security_migration": "documents.0005_ingestion_security",
      "columns": ["id", "tenant_id", "source_document_id", "source_version_id", "storage_object_id", "extract_authorization_id", "requested_by_actor_id", "status", "parser_version", "configuration_version", "locale", "attempt_count", "max_attempts", "lease_owner", "lease_expires_at", "heartbeat_at", "checkpoint", "input_manifest_sha256", "output_manifest_sha256", "reason_code", "quality_summary", "row_version", "created_at", "updated_at"],
      "keys": ["PRIMARY KEY (id)", "UNIQUE (tenant_id,id)", "UNIQUE active (tenant_id,source_version_id,parser_version,configuration_version)", "composite tenant foreign keys to document, version, storage object, and extract authorization"],
      "checks": ["fixed durable status", "locale = en", "canonical manifest hashes", "lease/result/quality shapes", "1 <= max_attempts <= 10", "attempt_count <= max_attempts"],
      "indexes": ["ix_ingest_runs_claim (tenant_id,status,created_at)", "ix_ingest_runs_source (tenant_id,source_version_id,created_at)", "uq_ingestion_runs_active_version partial unique"],
      "lifecycle": "durable leased extraction/normalization run; terminal scope and evidence are immutable",
      "rls": "ENABLE + FORCE"
    },
    {
      "schema": "integration",
      "table": "document_ingestion_attempts",
      "migration": "documents.0003_ingestion_schema",
      "security_migration": "documents.0005_ingestion_security",
      "columns": ["id", "tenant_id", "ingestion_run_id", "attempt_number", "outcome", "reason_code", "checkpoint", "input_manifest_sha256", "output_manifest_sha256", "observation", "started_at", "completed_at"],
      "keys": ["PRIMARY KEY (id)", "UNIQUE (tenant_id,id)", "UNIQUE (tenant_id,ingestion_run_id,attempt_number)", "FOREIGN KEY (tenant_id,ingestion_run_id) -> document_ingestion_runs(tenant_id,id)"],
      "checks": ["fixed outcome", "completion timestamp shape", "observation is object", "canonical manifest hashes"],
      "indexes": ["ix_ingest_attempts_run (tenant_id,ingestion_run_id,attempt_number)"],
      "lifecycle": "immutable completed attempt evidence with one running-to-terminal transition",
      "rls": "ENABLE + FORCE"
    },
    {
      "schema": "app",
      "table": "source_artifacts",
      "migration": "documents.0003_ingestion_schema",
      "security_migration": "documents.0005_ingestion_security",
      "columns": ["id", "tenant_id", "source_document_id", "source_version_id", "created_by_run_id", "artifact_role", "schema_version", "content_sha256", "byte_count", "json_payload", "text_payload", "created_at"],
      "keys": ["PRIMARY KEY (id)", "UNIQUE (tenant_id,id)", "UNIQUE (tenant_id,source_version_id,artifact_role,schema_version)", "composite tenant foreign keys to document, version, and ingestion run"],
      "checks": ["role in canonical_json/normalized_markdown", "canonical content hash", "positive bytes", "role-specific payload shape"],
      "indexes": ["ix_source_artifacts_version (tenant_id,source_version_id,artifact_role)"],
      "lifecycle": "immutable normalized-source projection; source text never enters audit/outbox facts",
      "rls": "ENABLE + FORCE"
    },
    {
      "schema": "app",
      "table": "document_pages",
      "migration": "documents.0003_ingestion_schema",
      "security_migration": "documents.0005_ingestion_security",
      "columns": ["id", "tenant_id", "source_document_id", "source_version_id", "created_by_run_id", "parser_version", "configuration_version", "page_number", "width_points", "height_points", "text", "text_sha256", "ocr_used", "created_at"],
      "keys": ["PRIMARY KEY (id)", "UNIQUE (tenant_id,id)", "UNIQUE (tenant_id,source_version_id,parser_version,configuration_version,page_number)", "composite tenant foreign keys to document, version, and ingestion run"],
      "checks": ["page_number >= 1", "positive finite page dimensions", "canonical text hash"],
      "indexes": ["ix_document_pages_version (tenant_id,source_version_id,page_number)"],
      "lifecycle": "immutable authoritative normalized page",
      "rls": "ENABLE + FORCE"
    },
    {
      "schema": "app",
      "table": "document_elements",
      "migration": "documents.0003_ingestion_schema",
      "security_migration": "documents.0005_ingestion_security",
      "columns": ["id", "tenant_id", "source_document_id", "source_version_id", "page_id", "created_by_run_id", "position", "kind", "text", "text_sha256", "created_at"],
      "keys": ["PRIMARY KEY (id)", "UNIQUE (tenant_id,id)", "UNIQUE (tenant_id,page_id,position)", "composite tenant foreign keys to document, version, page, and ingestion run"],
      "checks": ["position >= 1", "kind in heading/paragraph", "canonical text hash"],
      "indexes": ["ix_document_elements_page (tenant_id,page_id,position)"],
      "lifecycle": "immutable authoritative normalized element",
      "rls": "ENABLE + FORCE"
    },
    {
      "schema": "app",
      "table": "document_sections",
      "migration": "documents.0003_ingestion_schema",
      "security_migration": "documents.0005_ingestion_security",
      "columns": ["id", "tenant_id", "source_document_id", "source_version_id", "created_by_run_id", "position", "title", "start_page", "end_page", "created_at"],
      "keys": ["PRIMARY KEY (id)", "UNIQUE (tenant_id,id)", "UNIQUE (tenant_id,source_version_id,position)", "composite tenant foreign keys to document, version, and ingestion run"],
      "checks": ["position >= 1", "1 <= start_page <= end_page"],
      "indexes": ["ix_document_sections_version (tenant_id,source_version_id,position)"],
      "lifecycle": "immutable authoritative normalized section",
      "rls": "ENABLE + FORCE"
    },
    {
      "schema": "app",
      "table": "document_section_elements",
      "migration": "documents.0003_ingestion_schema",
      "security_migration": "documents.0005_ingestion_security",
      "columns": ["id", "tenant_id", "source_version_id", "section_id", "element_id", "position", "created_at"],
      "keys": ["PRIMARY KEY (id)", "UNIQUE (tenant_id,id)", "UNIQUE (tenant_id,section_id,position)", "UNIQUE (tenant_id,section_id,element_id)", "composite tenant foreign keys to version, section, and element"],
      "checks": ["position >= 1"],
      "indexes": ["ix_doc_section_elements (tenant_id,section_id,position)"],
      "lifecycle": "immutable ordered normalized section-to-element edge",
      "rls": "ENABLE + FORCE"
    }
  ]
}
